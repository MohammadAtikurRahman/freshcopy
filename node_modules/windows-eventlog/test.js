var EventLog = require('.').EventLog;
try {
	var el = new EventLog("eeo-webfrontend");
	el.logSync("error", "MIAU Sync");
	el.log("error", "MIAU Async", function(err, success) {
		if (err) throw err;
		console.log("done");
	});
} catch(e) {
	console.dir(e);
}
